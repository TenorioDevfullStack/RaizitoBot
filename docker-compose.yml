services:
  raizitobot:
    build: .
    container_name: raizitobot
    restart: unless-stopped
    ports:
      - "${ADMIN_PANEL_BIND:-127.0.0.1}:${ADMIN_PANEL_PORT:-8080}:${ADMIN_PANEL_PORT:-8080}"
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - TELEGRAM_WEBHOOK_SECRET=${TELEGRAM_WEBHOOK_SECRET:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-2.5-flash}
      - ASSISTANT_PERSONA=${ASSISTANT_PERSONA:-}
      - DB_PATH=${DB_PATH:-data/bot_data.db}
      - ADMIN_PANEL_ENABLED=${ADMIN_PANEL_ENABLED:-false}
      - ADMIN_PANEL_HOST=${ADMIN_PANEL_HOST:-0.0.0.0}
      - ADMIN_PANEL_PORT=${ADMIN_PANEL_PORT:-8080}
      - ADMIN_PANEL_TOKEN=${ADMIN_PANEL_TOKEN:-}
      - ADMIN_PANEL_BACKUP_DIR=${ADMIN_PANEL_BACKUP_DIR:-data/backups}
      - LOG_FILE=${LOG_FILE:-data/bot.log}
      - ENFORCE_AUTHORIZED_USERS=${ENFORCE_AUTHORIZED_USERS:-false}
      - AUTHORIZED_USER_IDS=${AUTHORIZED_USER_IDS:-}
      - RAG_VECTOR_BACKEND=${RAG_VECTOR_BACKEND:-sqlite}
      - RAG_SUPABASE_FALLBACK_TO_SQLITE=${RAG_SUPABASE_FALLBACK_TO_SQLITE:-true}
      - SUPABASE_URL=${SUPABASE_URL:-}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY:-}
      - SUPABASE_SCHEMA=${SUPABASE_SCHEMA:-public}
      - SUPABASE_KNOWLEDGE_TABLE=${SUPABASE_KNOWLEDGE_TABLE:-knowledge_items}
      - SUPABASE_MATCH_FUNCTION=${SUPABASE_MATCH_FUNCTION:-match_knowledge_items}
      - SUPABASE_REQUEST_TIMEOUT=${SUPABASE_REQUEST_TIMEOUT:-10}
      - GOOGLE_SEARCH_API_KEY=${GOOGLE_SEARCH_API_KEY:-}
      - GOOGLE_SEARCH_CX=${GOOGLE_SEARCH_CX:-}
      - GOOGLE_SERVICE_ACCOUNT_FILE=${GOOGLE_SERVICE_ACCOUNT_FILE:-/app/service_account.json}
      - GOOGLE_SERVICE_ACCOUNT_JSON=${GOOGLE_SERVICE_ACCOUNT_JSON:-}
      - GOOGLE_DELEGATED_USER=${GOOGLE_DELEGATED_USER:-}
      - GOOGLE_USE_DOMAIN_WIDE_DELEGATION=${GOOGLE_USE_DOMAIN_WIDE_DELEGATION:-false}
      - GOOGLE_GMAIL_USE_DELEGATION=${GOOGLE_GMAIL_USE_DELEGATION:-true}
      - GOOGLE_DRIVE_USE_DELEGATION=${GOOGLE_DRIVE_USE_DELEGATION:-false}
      - GOOGLE_CALENDAR_USE_DELEGATION=${GOOGLE_CALENDAR_USE_DELEGATION:-false}
      - GOOGLE_DOCS_USE_DELEGATION=${GOOGLE_DOCS_USE_DELEGATION:-false}
      - GOOGLE_GMAIL_USER_ID=${GOOGLE_GMAIL_USER_ID:-me}
      - GOOGLE_CALENDAR_ID=${GOOGLE_CALENDAR_ID:-primary}
      - GOOGLE_CALENDAR_TIMEZONE=${GOOGLE_CALENDAR_TIMEZONE:-America/Sao_Paulo}
      - GOOGLE_MAPS_API_KEY=${GOOGLE_MAPS_API_KEY:-}
      - GOOGLE_MAPS_LANGUAGE=${GOOGLE_MAPS_LANGUAGE:-pt-BR}
      - MCP_TRANSPORT=${MCP_TRANSPORT:-stdio}
      - APP_USERNAME=${APP_USERNAME:-}
      - APP_PASSWORD=${APP_PASSWORD:-}
      - CUSTOM_API_URL=${CUSTOM_API_URL:-https://pro.raizeletrica.com.br}
    volumes:
      - ./data:/app/data
      - ./service_account.json:/app/service_account.json:ro
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
